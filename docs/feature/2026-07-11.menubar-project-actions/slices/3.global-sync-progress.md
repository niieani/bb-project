## Parent

BB-blmfyefe — read and understand it first before starting work.

## What to build

Extend global sync through the structured operation stream so the menubar shows which repository is active, useful phase text, and completed/total progress while preserving one-operation locking, row-specific failures, and automatic refresh.

## Acceptance criteria

- Global sync emits ordered repository-attributed progress and completed/total counts without requiring clients to parse human logs.
- The active repository shows a spinner; the footer presents one concise phase/count line and useful failure attribution.
- Controls remain serialized and final live state refreshes after success or failure.
- Global progress tests cover ordering, attribution, counts, errors, and Swift presentation/model behavior; related docs are current.

## Blocked by

- BB-lmwqykdw
