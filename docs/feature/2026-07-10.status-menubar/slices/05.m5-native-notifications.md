# M5 — BB-gcpevkel

Make the app the sole native notifier and delete all Go delivery compatibility paths. Red notification/persistence/routing tests first; launch-at-login and live native verification; full gates; fp report/commit/assignment.

Implementation boundary: Go exports eligibility, whole-fleet fingerprint, and throttle minutes in the stable status contract. Swift owns permission, persistence, throttled delivery, and click routing without reproducing attention policy. The owner approved starting implementation while M4 waits on Apple notarization; M4 and M5 still close in dependency order after signed-app proof.
