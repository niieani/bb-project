# M3 — BB-oqeftwks

Add concise menu sections, partial failures, interval/wake refresh, and detached Sync now. Red external behavior tests first; live Computer Use verification; full gates; fp report/commit/assignment.

Status supplies title and eligibility. Overview independently verifies remote-machine cells; Swift does not infer attention policy. Refresh events are injected as an async stream so interval and wake use one path. Sync always refreshes afterward, including when `bb sync` exits non-zero because blocked repositories remain.
